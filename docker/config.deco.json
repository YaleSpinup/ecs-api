{ 
  "listenAddress": ":8080",
  "accounts": {
    "spinup": {
      "region": "us-east-1",
      "akid": "{{ .spinup_akid }}",
      "secret": "{{ .spinup_secret }}",
      "default_sgs": ["{{ .default_sg }}"],
      "default_subnets": ["{{ .default_subnet }}"]
    }
  },
  "token": "{{ .api_token }}",
  "logLevel": "{{ .log_level }}"
}
